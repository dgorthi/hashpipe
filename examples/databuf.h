/* This will provide a link for hashpipe to know where the buffer is defined.
 * When you have more complicated data structures, you can define them here. 
*/
hashpipe_databuf_t *databuf_create(int instance_id, int databuf_id);
